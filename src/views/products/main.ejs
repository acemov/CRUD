<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/normalize.css">
    <title>Primera vista 😎</title>
</head>

<body>

    <!-- ----------------- SQUARE ----------------- -->
    <%- include("../partials/cuadrado.ejs") %>
    <!-- -----------------Muestra los cuadrados----------------- -->
    <h2>Cuadrados 👇</h2>
    <div class="caja">
    <% for(let i=0; i< 10; i++){ %> <!-- Este "i" se declara en esta linea de codigo -->
        <% if(i%2==0) {%>
            <div class="par">Soy el numero: <%=i+1%></div>
        <% } else {%>
            <div class="impar">Soy el numero: <%=i+1%></div>
        <% } %>
    <% } %>
    </div>
    <!-- -----------------Muestra la lista de persona----------------- -->
    <h2>Lisa de personas 👇</h2>
    <ul class="listaDeNombres" type="none">
        <% for(let i=0; i< listaDeNombres.length; i++){ %>
            <li class="nombres">👉<%=listaDeNombres[i]%>👈numero de lista: <%=i%></li>
            <% } %>
        </ul>
    <!-- ----------------- Formulario metodo post ----------------- -->
    <h2>Envia un formulario por el metodo POST con este link 👇 </h2>
    <div class="volver-a">
        <a class="volver" href="http://localhost:3000/enviar">SOY EL LINK</a>
    </div>
    <!-- ----------------- Lo que se se busco ----------------- -->
    <h2>Envia un formulario por el metodo GET con este link 👇 (el que aparece en la URL)</h2>
    <ul type="none" class="listaDeNombres">
        <li>
            ⭐<%= Primera_Busqueda %>
        </li>
        <li>
            ⭐<%= Segunda_Busqueda %>
        </li>
    </ul>
    <div class="volver-a">
        <a class="volver" href="http://localhost:3000/buscar/ingresa">SOY EL LINK:)</a>
    </div>
</body>

</html>